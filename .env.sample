# AI Provider Settings (choose one)
# Options: lmstudio, ollama, openai_compatible
AI_PROVIDER=lmstudio

# AI API settings (auto-detected based on provider if not set)
# AI_API_URL=http://localhost:1234/v1    # LMStudio
# AI_API_URL=http://localhost:11434      # Ollama
# AI_API_KEY=your-api-key                # Optional for local providers
# AI_MODEL=llama2                        # Optional: specific model name

# Legacy LMStudio settings (for backward compatibility)
LMSTUDIO_API_URL=http://localhost:1234/v1
LMSTUDIO_API_KEY=lm-studio

# Data folder path
DATA_FOLDER=./data

# Vector database settings
VECTOR_DB_PATH=./vector_db
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Embedding model
EMBEDDING_MODEL=all-MiniLM-L6-v2
